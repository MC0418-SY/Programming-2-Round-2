#define MAX 3

typedef struct {
  int x;
  int y;
} MyPoint;

typedef struct {
  MyPoint list[MAX];
  int count;
} PointList;

typedef struct {
  MyPoint list[MAX];
  int count;
} PointListDynamic;

MyPoint point;

point.x = 5;
point.y = -6

PointList pList;
plist.count = 0;
pList.list[plist.count++] = point;


pointListDynamic myList;
myList.list = malloc(sizeof(MyPoint) * 10);
myList.count = 0;
